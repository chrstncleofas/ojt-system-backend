# Example .env.local for backend
NODE_ENV=development
PORT=3000

# MongoDB Connection (target database)
# Example (replace <user> and <password>):
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.dcmdo1a.mongodb.net/ojtsystem?retryWrites=true&w=majority

# Optional: source DB URI used by migration script (tkms)
# SOURCE_MONGO_URI=mongodb+srv://<user>:<password>@cluster0.dcmdo1a.mongodb.net/tkms?retryWrites=true&w=majority

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key
